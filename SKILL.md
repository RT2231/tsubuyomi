---
name: tsubuyomi
description: 法令(条文)や論文を、ユーザーと対話しながら省略せず1条ずつ・1段落ずつ精読するためのスキル。ユーザーが「粒読みを使って」「つぶよみを使って」「tsubuyomiを使って」「/tsubuyomi」のようにこのスキルを明示的に指定して呼び出したときだけ使うこと。ユーザーが指定していない場面では、法令や論文の話題が出ても自動では使わない。対応入力:e-Gov法令検索のXML/HTML/RTF/PDF横一段、論文などのPDF、URL、貼り付けテキスト。
---

# 粒読み(つぶよみ):法令・論文を一粒ずつ対話で精読する

法令や論文を、ユーザーと一緒に「省略せず、区切りながら、対話で」読み解くスキルです。
要約して終わらせるのではなく、原文をすべて確認しながら、ユーザーが納得して先へ進めることを目的にします。

## 発動条件

ユーザーがこのスキルを明示的に指定したときだけ使います。
法令や論文が話題に出ただけでは発動しません。

## 進め方の原則

- **省略しない**:原文は必ず全文を示す。「中略」「以下同様」「〜など」で削らない。附則・別表・注・改正履歴・参考文献・付録も対象。
- **区切って進む**:1回の応答で扱うのは1つの区切り(下記)だけ。
- **ユーザーのペースで進む**:「次へ」などの指示があるまで先に進まない。質問・脱線・前の条に戻る依頼には付き合い、終わったら元の位置へ戻る。
- **原文と解説を混ぜない**:原文は原文として正確に引用し、解説・言い換えは別に示す。原文を勝手に言い換えたり補ったりしない。
- **断定しすぎない**:解釈が分かれる点は「解釈が分かれうる」と明示する。法的助言や実務判断は行わない。

## 開始時の手順

1. **入力を確認する**:ファイルがアップロードされていれば `/mnt/user-data/uploads/` から読む。形式ごとの読み取り方は `references/formats.md` を参照。URLなら取得、貼り付けならそのまま使う。
   - 同じ法令が複数の形式で渡された場合は、XMLを正とし、HTMLなどで表やルビを照合する。
   - 形式ごとに、そのまま読むと原文が欠ける・崩れる箇所がある(RTFのルビ親文字、PDFの表の順序など)。必ず `references/formats.md` の「落とし穴」に沿って前処理・確認する。
2. **原文を読み込んで全体構造を把握する**:編・章・節・条(論文なら章・節・段落)の構成を確認する。読み込んだ条数と、目次や資料上の総数が合っているかも確かめる。
3. **最初に一度だけ**、次を伝える:
   - 私は法律の専門家ではなく、解説は理解を助ける目的であって法的助言ではないこと(法令の場合)
   - 全体の構成(条数、附則や別表の有無など)と、読み進める区切りの単位
4. **最初の区切りに入る**。

## 区切りの単位

- **法令**:原則1条ずつ。条が長い場合(項が多い場合)は、項のまとまりごとに分けてよい。枝番号の条(第○条の二 など)は独立した条として扱う。
  章・節の冒頭では、その章・節の見出しと位置づけを先に一言添える。
  附則・別表も、飛ばさず同じ流れで読む。
- **論文**:原則1段落ずつ、または意味のまとまり(小節)ごと。図表・式・脚注も対象に含める。

## 1つの区切りの出力形式

### 法令の場合(1条ごと)

1. **見出しと位置づけ**:条番号、見出し(条文見出し)、その条が全体の中で何をしているかを一言で。
2. **項・号ごとに、次を繰り返す**:
   - **原文**:その項(または号)の原文を省略せずそのまま引用する。項番号・号番号も原文のとおりに残す。
   - **平易な言い換え**:誰が・何を・どんな場合に・どうなるか、がわかる言葉で。
   - **用語解説**:その項・号に出てきた、つまずきやすい用語や、他の条を参照している箇所(「前条の」「第○条第○項に規定する」など)を説明する。参照先が必要なら、その原文も確認して示す。
3. 最後に、次に進む前の一言を添える(例:「ここまでで気になる点があれば聞いてください。なければ『次へ』と伝えてください」)。

応答の冒頭には、毎回「粒マップ」を置く(詳しくは下の「粒マップ」の節)。わからなかった点は「つまずきメモ」に記録する。論文の場合も同じ。

### 論文の場合(1段落ごと)

1. **位置づけ**:その段落が論文全体(章・節)の中で何をしているか。
2. **原文**:段落を省略せず引用する(著作権に配慮し、ユーザーが提供した資料の範囲で扱う)。
3. **平易な言い換え**:主張・根拠・前提が何かがわかるように。
4. **用語解説**:専門用語、略語、式の記号、参照されている図表・文献の役割。

## 粒マップ(進み具合の見える化)

読み終えた区切りを「粒」に見立てて、毎回の応答の冒頭に現在位置を示す。

例(全10条で第3条を読んでいるとき):

```
●●◉○○○○○○○  第3条 / 全10条
```

- ● = 読み終えた粒、◉ = いま読んでいる粒、○ = これから読む粒。
- 区切りが多い場合(目安として20を超えるとき)は、粒を10個に縮めて割合で表し、横に「第85条 / 全1050条」のように実際の位置を添える。
- 章・節・附則・別表があれば、粒マップの下に「第2章 第3節」のように所属を一行で添える。
- ユーザーが「第○条から」「附則へ飛ぶ」と言ったら、その位置へ移動し、粒マップもそれに合わせる。読み飛ばした範囲は粒を○のままにして、後で戻れるようにする。
- 途中で会話が中断しても、粒マップを見れば「どこまで読んだか」がわかり、そこから再開できるようにする。

## つまずきメモ(わからなかった点の記録)

ユーザーが「わからない」「もう一度」「難しい」と言った箇所や、用語について質問した箇所を、会話の中でメモしておく。

- **記録するもの**:用語、他の条への参照、解釈がむずかしかった項・号。一件につき「どこで(条・項)」「何につまずいたか」を短く残す。
- **見せるタイミング**:
  1. 章・節の終わり(区切りの節目)で、その章・節でたまった分を見せる
  2. 全部読み終えたときに、全体の一覧を見せる
  3. ユーザーが「つまずきメモ」と言ったとき、いつでも見せる
- **見せ方**:短い箇条書きにし、解消済みのものには印をつける。復習したいものがあれば、その箇所の原文と解説をもう一度出す。
- **ファイル化**:読み終えたときに、希望があれば一覧をMarkdownファイルにして渡せると伝える。勝手にファイルは作らない。
- ユーザーが「その用語は知っている」と言った語はメモに入れない。

## 参考:応答のトーン

- 丁寧だが硬すぎない口調にする。
- ユーザーの理解度に合わせ、すでに知っている用語まで説明しすぎない。ユーザーが「その用語は知っている」と言ったら、以降は省く。
- 解説が長くなるときも、原文の全文引用は削らない。
